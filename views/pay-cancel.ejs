<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Оплата отменена — Wayfis</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    <% var accentHex = (typeof settings !== 'undefined' && settings.theme_accent) || '#10B981'; var _r = parseInt(accentHex.slice(1,3),16); var _g = parseInt(accentHex.slice(3,5),16); var _b = parseInt(accentHex.slice(5,7),16); var _hr = Math.max(0,_r-20); var _hg = Math.max(0,_g-20); var _hb = Math.max(0,_b-20); %>
    :root { --accent: <%= accentHex %>; --accent-rgb: <%= _r %>,<%= _g %>,<%= _b %>; --accent-hover: #<%= _hr.toString(16).padStart(2,'0') %><%= _hg.toString(16).padStart(2,'0') %><%= _hb.toString(16).padStart(2,'0') %>; }
    body {
      font-family: 'Inter', -apple-system, sans-serif;
      background: #0E0F10; color: #fff;
      min-height: 100vh; display: flex; align-items: center; justify-content: center;
    }
    .card {
      background: #1A1C1E; border: 1px solid rgba(255,255,255,0.06);
      border-radius: 20px; padding: 48px 40px; max-width: 480px; width: 90%;
      text-align: center;
    }
    .icon {
      width: 72px; height: 72px; border-radius: 50%;
      background: rgba(239,68,68,0.15); margin: 0 auto 24px;
      display: flex; align-items: center; justify-content: center;
    }
    .icon svg { width: 36px; height: 36px; stroke: #ef4444; }
    h1 { font-size: 24px; font-weight: 800; margin-bottom: 8px; }
    p { color: rgba(255,255,255,0.6); font-size: 14px; line-height: 1.6; }
    .btn {
      display: inline-flex; align-items: center; gap: 8px;
      padding: 14px 28px; border-radius: 12px; text-decoration: none;
      font-size: 14px; font-weight: 700; font-family: inherit; cursor: pointer;
      margin: 20px 8px 0;
    }
    .btn-primary { background: linear-gradient(135deg, #10B981, #059669); color: #fff; }
    .btn-outline { background: rgba(255,255,255,0.06); color: rgba(255,255,255,0.6); border: 1px solid rgba(255,255,255,0.1); }
    @media (max-width: 480px) { .card { padding: 32px 20px; } .icon { width: 56px; height: 56px; margin-bottom: 16px; } .icon svg { width: 28px; height: 28px; } h1 { font-size: 20px; } .btn { padding: 12px 24px; font-size: 13px; margin: 12px 4px 0; } }
  </style>
</head>
<body>
  <div class="card">
    <div class="icon">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
      </svg>
    </div>
    <h1>Оплата не завершена</h1>
    <p>Ты отменил оплату или произошла ошибка. Попробуй ещё раз.</p>
    <a href="/" class="btn btn-primary">Вернуться в магазин</a>
    <a href="/" class="btn btn-outline">Повторить</a>
  </div>
</body>
</html>
